import * as React from "react";
import SoundFontPlayer from "soundfont-player";
export declare const getAudioContext: () => any;
export declare type InstrumentState = {
    isLoading: boolean;
    instrument: SoundFontPlayer.Player | null;
    audioContext: ReturnType<typeof getAudioContext>;
    playNoteFor: null | ((noteName: string, duration: number) => Promise<void>);
    playNoteIn: null | ((noteName: string, when: number, duration: number) => Promise<void>);
};
export declare type InstrumentProps = {
    name: SoundFontPlayer.InstrumentName;
    renderLoading: () => any;
    children: ({ instrument, audioContext }: InstrumentState) => any;
};
export declare class ReactSoundFontPlayer extends React.Component<InstrumentProps, InstrumentState> {
    static defaultProps: {
        name: string;
        renderLoading: () => string;
    };
    state: {
        instrument: null;
        audioContext: any;
        isLoading: boolean;
        playNoteIn: null;
        playNoteFor: null;
    };
    shouldComponentUpdate(nextProps: InstrumentProps): boolean;
    componentDidMount(): Promise<void>;
    render(): any;
}
export default ReactSoundFontPlayer;
